{
  "name": "pmvl-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shows what a prediction-market order actually costs, on the order ticket.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && node scripts/bundle.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.6.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
