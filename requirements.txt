# API RUNTIME dependencies only.
#
# This file is what the Vercel Python function installs, so it is deliberately the
# minimal set the read-only API needs. The quant stack (numpy/pandas/scipy/sklearn/
# statsmodels) and the worker/test tooling live in requirements-dev.txt: nothing the
# API serves needs them, and excluding them keeps the serverless bundle small.
#
# Local development installs requirements-dev.txt, which includes this file.
pydantic==2.12.4
pydantic-settings==2.12.0
SQLAlchemy==2.0.44
python-dotenv==1.2.1
httpx==0.28.1
tenacity==9.1.2
fastapi==0.121.1
