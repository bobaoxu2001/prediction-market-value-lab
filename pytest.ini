[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: exercises the database, pipelines or API (still no live network)
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
